import { createContext } from "react";

const AddVehicleContext = createContext(null);
const RequestRideContext = createContext(null);
const GiveRideContext = createContext(null);

export { AddVehicleContext, RequestRideContext, GiveRideContext };
